#include <stdio.h>
#include <math.h>

int main(void)
{
    float a, b, c, x1, x2, delta;

    printf("Insira os valores de a, b e c: ");
    scanf("%f %f %f", &a, &b, &c);

    if (a == 0)
    {
        
    }
    


    return 0;
}

